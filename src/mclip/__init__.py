"""mclip — MCP-CLI Protocol: consolidate CLIs into MCP tools via introspection.

This package provides an MCP server that automatically discovers CLI tool
capabilities by parsing ``--help`` output, ``man`` pages, and shell completion
scripts, then exposes them as structured MCP tools for agent consumption.

.. moduleauthor:: Kamil Rybacki
"""
